# Copy to .env (local) and add the same values to Netlify env vars in production.
# Do NOT commit .env.

# OpenAI — Day 3
OPENAI_API_KEY=sk-...

# Stripe — Day 5
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...

# Site URL — used by Stripe success/cancel redirects
URL=http://localhost:8888
